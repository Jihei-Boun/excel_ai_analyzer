[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    benchmark_live: live Ollama AnalysisPlan benchmark (opt-in; skipped by default)
    benchmark_multi_live: live Ollama multi-file Integration Pipeline benchmark (opt-in)
